import { Dashboard } from '@/components/dashboard';

export const metadata = {
  title: 'Dashboard - ExamMaster',
  description: 'Manage your exams and view student results',
};

export default function DashboardPage() {
  return <Dashboard />;
}
